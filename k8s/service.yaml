apiVersion: v1
kind: Service
metadata:
  name: phlower
  labels:
    app: phlower
spec:
  selector:
    app: phlower
  ports:
    - port: 80
      targetPort: 8100
      protocol: TCP
  type: ClusterIP
